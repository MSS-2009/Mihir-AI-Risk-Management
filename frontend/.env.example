# Copy to frontend/.env.local for local dev.
# In production (Vercel), set NEXT_PUBLIC_API_URL to your deployed backend URL,
# e.g. https://avenoir-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:8000
